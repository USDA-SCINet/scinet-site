{
  "name": "scinet",
  "private": true,
  "author": "Moe Richert",
  "license": "CC0-1.0",
  "scripts": {
    "start": "node _scripts/infolog.js && bundle exec jekyll serve",
    "dev": "node _scripts/infolog.js && bundle exec jekyll serve & npx gulp watch",
    "update-dep": "npm install && bundle install && npx gulp update",
    "start-detached": "node _scripts/infolog.js && bundle exec jekyll serve --detach",
    "setup-jekyll": "bundle",
    "watch-start": "npm run dev",
    "update-mod": "bash ./_scripts/git-modified-date.sh",
    "pa11y-ci": "pa11y-ci --threshold=7"
  },
  "dependencies": {
    "@uswds/uswds": "^3.7.1"
  },
  "devDependencies": {
    "@uswds/compile": "^1.0.0"
  }
}
