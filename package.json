{
  "name": "scinet",
  "private": true,
  "author": "Moe Richert",
  "license": "CC0-1.0",
  "scripts": {
    "start": "bundle exec jekyll serve",
    "dev": "bundle exec jekyll serve & npx gulp watch",
    "update-dep": "npm install & bundle install & npx gulp update",
    "start-detached": "bundle exec jekyll serve --detach",
    "setup-jekyll": "bundle",
    "watch-start": "bundle exec jekyll serve & npx gulp watch",
    "pa11y-ci": "pa11y-ci --threshold=7"
  },
  "dependencies": {
    "@uswds/uswds": "^3.4.1"
  },
  "devDependencies": {
    "@uswds/compile": "^1.0.0",
    "pa11y-ci": "^3.0.1"
  }
}
