# Set the default behavior, in case people don't have core.autocrlf set.
* text=auto

# Convert line endings to LF for all Markdown files
*.md text eol=lf

# Declare files that will always have CRLF line endings on checkout.
*.sln text eol=crlf

# Denote all files that are truly binary and should not be modified.
*.png binary
*.jpg binary
*.webp binary
*.pdf binary

# Exclude specific directories and files from text conversion
assets/* -text